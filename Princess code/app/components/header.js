import React from 'react';
import {Platform, StyleSheet, Text, View} from 'react-native';


//Make a component
const Header = props => {
    return <Text>Florida</Text>;
};

//Make component available to other parts of the app
export default Header;